export class Pokemon {
    name: string;
    img: string;
    desc: string;

    constructor(name: string, img: string, desc: string) {
        this.name = name;
        this.img = img;
        this.desc = desc;
    }
}